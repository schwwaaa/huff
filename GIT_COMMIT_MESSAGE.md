# HUFF Classic Pass 22 — Git Commit Message

```bash
git add . && git commit \
  -m "perf: streamline HUFF Classic scanline preparation" \
  -m "Continue from the runtime-confirmed Pass 21 baseline without changing decoder ownership, frame scheduling, temporal history, effect order, or output transport." \
  -m "Select neutral and active Scanline drift/shift preparation variants before the band loop while preserving exact p5 noise call count and order." \
  -m "Cache phase and focus scalars per pass and prepare direct full-cross rectangles when SHIFT and SKEW are neutral." \
  -m "Resolve prepared typed arrays once before dispatch and cache transform constants by render geometry and angle." \
  -m "Use a direct exact-horizontal Canvas2D path that restores only globalAlpha, avoiding save, two translations, and restore while retaining the transformed path for every nonzero angle." \
  -m "Add profiler-only Scanline geometry, preparation-cache, and direct/transformed path telemetry." \
  -m "Preserve the stable Blob URL decoder, independent clocks, Pass 13S lifecycle, Pass 16S Syphon bootstrap, Pass 19 Syphon control plane, Spout, Rust transport, and mandatory framework packaging." \
  -m "Add deterministic Pass 22 validation with 12,000 cases and more than 3.8 million exact prepared-band field comparisons."
```
