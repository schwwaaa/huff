# HUFF Classic Pass 21 — Git Commit Message

```bash
git add . && git commit \
  -m "perf: cache HUFF Classic Flow field preparation" \
  -m "Continue from the runtime-confirmed Pass 20 baseline without changing decoder ownership, frame scheduling, effect order, temporal history, or output transport." \
  -m "Add a persistent typed Flow field workspace keyed by grid generation, effective SPREAD frequency, and SWIRL." \
  -m "Cache primary and turbulence noise-coordinate products, radial SWIRL sine/cosine values, and maximum source clipping bounds outside ordinary per-tile frames." \
  -m "Resolve Flow typed-array references and animated constants once per pass while preserving p5 noise calls, floating-point operation order, Math.fround quantization, source/destination rectangles, and tile paint order." \
  -m "Add profiler-only Flow frequency and SWIRL cache rebuild/reuse telemetry." \
  -m "Preserve the stable Blob URL decoder, independent clocks, Pass 13S lifecycle cleanup, Pass 16S Syphon bootstrap, Pass 19 Syphon control plane, Spout, Rust transport, and mandatory framework packaging." \
  -m "Add deterministic Pass 21 validation with 5,000 Flow cases and more than 4.4 million exact comparisons."
```
