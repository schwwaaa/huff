source "https://rubygems.org"

# Only needed for local preview — GitHub Pages uses remote_theme directly.
# To preview locally:
#   gem install bundler
#   bundle install
#   bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "jekyll-remote-theme"
