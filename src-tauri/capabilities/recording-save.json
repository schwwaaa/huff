{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "recording-save",
  "description": "Allow saving recorded video via dialog + fs from any app window",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:default"
  ]
}
