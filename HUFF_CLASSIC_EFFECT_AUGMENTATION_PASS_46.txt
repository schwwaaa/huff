HUFF CLASSIC — EFFECT AUGMENTATION / OPTIMIZATION PASS 46
Threshold Solarize GPU Acceleration + Serial Stage Timing

BASELINE
- Direct continuation of HUFF Classic Pass 45.
- Pass 41A remains the accepted Classic foundation.
- Pass 42 Luma Quantize and Pass 43 Fluidity remain visually accepted/protected.
- Pass 44 active-parameter UI remains retained.

RUNTIME REPORT THAT TRIGGERED THIS PASS
- Pass 45 was closer, but the supplied runtime screenshot still showed ~48 FPS.
- Crucially, that screenshot had Solarize MODE=THRESHOLD, not LUMA QUANTIZE.
- Pass 45 accelerated only LUMA QUANTIZE, so the active THRESHOLD path still paid
  the older <=640px Canvas2D getImageData -> JavaScript pixel loop -> putImageData
  cost every render.

CHANGES
1. THRESHOLD Solarize now gets first refusal on the same bounded WebGL 1 helper.
   - Same <=640px Classic working ceiling.
   - No WebGL readPixels.
   - No new full-resolution framebuffer.
   - No synchronous Solarize getImageData / JS pixel loop / putImageData on GPU success.
   - Existing THRESHOLD equation is reproduced in byte-domain shader math.
   - Existing CPU path remains automatic fallback.

2. Existing LUMA QUANTIZE GPU path remains intact.
   - Quantize and Threshold now report separate profiler counters.

3. Profiler adds actual serial-stage wall-clock timings.
   - persistence;
   - front stage (Corrupt/Luma/Scan priority block);
   - direct Global Mix;
   - Feedback;
   - Flow;
   - Symmetry;
   - Solarize;
   - presentation.

PROTECTED
- Solarize THRESHOLD CPU transfer helpers are byte-identical to Pass 45.
- Solarize LUMA QUANTIZE CPU transfer helpers are byte-identical to Pass 45.
- Solarize FLUIDITY coefficient/presentation behavior is byte-identical.
- Pipeline Luma remains on the accepted CPU path; the rejected GPU Luma prototype is still absent.
- Feedback/Persistence equations are not changed.
- Flow remains frozen and byte-identical.
- factory presets and native Tauri/Syphon/Spout runtime are unchanged.

NO FRAME-CADENCE OPTIMIZATION
- no frame skipping;
- no every-N-frame Solarize gate;
- no playback-speed change;
- no sample-and-hold behavior.

RUNTIME ACCEPTANCE TEST
Recreate the exact Pass 45 screenshot state first:
- 1920x1080 Classic processing;
- heavy Feedback/Persistence;
- Luma Key ON / COMPOSITE / LIVE;
- Global Mix ON / AFTER FB (same blend/amount as the saved patch);
- Solarize ON / THRESHOLD / FLUIDITY 100;
- keep every other effect at the exact same ON/OFF state.

Open the profiler with ` and verify:
- gpu color = ON;
- gpu thresh advances continuously;
- gpu fall remains 0;
- sol read/xform/upload remain near zero/no CPU samples on the successful GPU branch;
- stage solar reports actual Solarize wall time;
- stage front / stage fb / stage pres identify the remaining serial cost if FPS is still below target.

Runtime acceptance remains user-owned on the target Mac/WebView.
