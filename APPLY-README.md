# Apply HUFF Native Milestone 18

The changed-files archive is intended for a clean Milestone 17 tree. Copy it over the project root while preserving directory structure.

Milestone 18 adds `state_documents.rs`, the State Library interface, four typed state-document workflows, scoped recall, source/transport references, automation and controller-map project storage, state-model validation, version metadata, and documentation.

The complete archive is safer when the Milestone 17 tree contains local modifications or when file additions are uncertain.
